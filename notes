need some dependencies 
express ,mongoose,cors,dotenv to run the application .
ToDoAPP: it's a database name or collection name which will be created on mongoDB server 

Server running on port 5001 
and connected to db 

We have to create routes what we need 
localhost:5001 
it will say json sent data 
now everything is working 


Use postman to send the data 


ading id in req.body if id matches then it will going to delete 
if they didn't give any size in that case it will display first page with default size 10

-2. GET /users?page=3&size=20
return us page 3 with size 20 records i.e skipping first 40 records



